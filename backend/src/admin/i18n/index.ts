// Medusa Admin already has built-in Mongolian (mn) translations
// Users can select Монгол from Settings > Profile in the admin dashboard
// No custom translations needed for the core admin interface

export default {}